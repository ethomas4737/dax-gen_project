# Journal

Append-only chronology. One row per event. Write with `Edit` (append) never with `Write` (overwrite).

Event categories: `session` (session-start / -resume / -end), `plan` (plan revision), `step` (step-start / -complete / -blocked), `decision`, `dispatch` (agent call — append cost block at end of summary), `qa` (QA result), `ops` (infra action, e.g., commits, pushes, DCC ops), `promote` (spike→src move), `note` (anything else).

For `dispatch` rows, append a cost block at the end of the summary: `(model=…, tokens=…, tool_uses=…, wall=…s)`.

| Timestamp (ET) | Category | Step | Summary |
|---|---|---|---|
| 2026-07-09 | ops | — | Scaffolded `dax-gen_project` from `dax-demo` template (moved template content up one level, dropped redundant nested harness/tools clones, fresh git init). Pinned harness SHA `f29e04997e9e310fd0cf26e330cc87f4d8e08845`. |
| 2026-07-09 | note | — | Drafted Phase 1 spec: curate D-SCRIPT PPI, AVIDa-hIL6+hTNFa antibody, and MLAEP viral-evolution datasets separately; produce EDA per dataset. Downstream modeling task deliberately deferred. |
| 2026-07-09 | note | — | Verified D-SCRIPT human PPI pairs (15,816 unique proteins) via mygene.info: 0 COVID/coronavirus hits; 1 non-functional IGHV pseudogene entry (33/474,517 pairs, all negative labels) — kept, per human decision. |
| 2026-07-09 | ops | — | Updated README.md + CLAUDE.md for the real project name/goal; renamed `docs/walkthrough.md` → `docs/dax-demo-walkthrough.md`; refreshed dax-state placeholders. |
| 2026-07-09 | plan | — | Drafted `plan-phase1.md` (9 steps: env verify, fetch+QA x3 datasets, EDA+QA). Human approved. |
| 2026-07-09 | step | 0 | Env verify done: existing user micromamba env `eda` has pandas/numpy/matplotlib/seaborn/requests; no new installs needed (fetch via plain HTTP, no `datasets`/`huggingface_hub` required). |
| 2026-07-09 | step | 1 | D-SCRIPT PPI data fetched into `rawdata/ppi/` (commit `23cbb0f`); all 13 files present, row counts match. See runs/phase1-1.md. |
| 2026-07-09 | qa | 1-qa | PASS. Label format inconsistency found: human files use int `0`/`1`, other species use float `0.0`/`1.0` — EDA must normalize. |
| 2026-07-09 | step | 2 | AVIDa-hIL6 + AVIDa-hTNFa fetched into `rawdata/avida/` (HF revisions pinned); hTNFa license confirmed CC-BY-NC-4.0. See runs/phase1-2.md. |
| 2026-07-09 | qa | 2-qa | PASS. 0 SARS-CoV-2 Ag_labels in either file. Column order differs between hIL6/hTNFa files (label/Ag_label swapped) — EDA must read by name. |
| 2026-07-09 | step | 3 | MLAEP 7 preprocessed files fetched into `rawdata/mlaep/` (commit `cbd21f4`). See runs/phase1-3.md. |
| 2026-07-09 | qa | 3-qa | PASS. All files size-verified. Noted `merged_all.jsonl` is generic (non-COVID) structural data, distinct from the other 6 COVID-specific files. |
| 2026-07-09 | step | 4 | EDA generated for all 3 datasets: `docs/eda-{ppi,avida,mlaep}.md` + 8 figures. Positive fraction reported per spec (PPI ~9.09% fixed ratio; AVIDa hIL6 3.66%/hTNFa 12.22%; MLAEP ACE2-bind 8.05% + 8 per-clone escape fractions). See runs/phase1-4.md. |
| 2026-07-09 | qa | 4-qa | PASS. Independently recomputed 3 headline positive-fraction numbers by separate method (awk / fresh pandas read) — exact match. All 8 figures valid PNGs; reports clean of leftover errors. |
| 2026-07-09 | note | — | Built `docs/phase1_eda_walkthrough.ipynb` — human follow-along notebook consolidating steps 1-4, executed end-to-end (16 code cells, 0 errors) with the `eda` conda env registered as a Jupyter kernel. Re-verifies exclusions live. See runs/phase1-notebook.md. |
| 2026-07-09 | note | — | Added "PLM-readiness" section to the walkthrough notebook (human plans to use a PLM downstream): context-window fit (all clear), vocabulary check (PPI has U/X in ~0.1-0.3% of seqs), positive-fraction-vs-length-bin. **New finding: PPI positive fraction is not flat by pair length** (0.165 shortest decile vs ~0.07-0.08 mid-range vs 0.103 longest) — a real shortcut-learning risk flagged for the downstream modeling phase. 21 code cells, 0 errors. See runs/phase1-notebook.md. |
| 2026-07-09 | note | — | Drafted `docs/phase1_eda_summary.md` — consolidated cross-dataset EDA report (TL;DR, per-dataset numbers, length-confound writeup, PLM-readiness table, data-quality notes, recommendations for Phase 2 scoping). Adds 2 new saved figures (`plm_length_vs_context_window.png`, `plm_ppi_positive_fraction_by_length.png`) previously only inline in the notebook. |
| 2026-07-09 | note | — | Ran the train/test identity-leakage check (highest-priority PLM check) on PPI. **Significant finding: 100% of `human_test`'s 15,525 unique proteins (by ID and by exact sequence) already appear in `human_train`** — zero genuinely unseen test proteins; the split holds out pairings, not proteins. 89/52,725 test pairs (0.17%) are exact train-pair duplicates. Added as notebook §5 (22 code cells, 0 errors) and promoted to the #1 finding in `docs/phase1_eda_summary.md`'s TL;DR/recommendations. See runs/phase1-notebook.md. |
| 2026-07-09 | decision | — | Human correctly pushed back: assumed the D-SCRIPT human train/test split was designed to test generalization, contradicting the framing of the prior entry. Checked D-SCRIPT's README + published methods: the project's actual generalization claim is cross-species transfer (human-trained model → mouse/fly/yeast/worm/ecoli), not human-split protein disjointness. Corrected the notebook §5 and `docs/phase1_eda_summary.md` framing: the 100% protein-overlap finding stands (verified, unchanged), but it reflects an intentional pair-level (interactome-completion) split design, not a benchmark flaw. Narrowed the practical implication to: only matters if downstream work needs a *novel-human-protein-specific* generalization claim, which would require a custom protein-disjoint split. |
| 2026-07-09 | note | — | Added `docs/phase1_eda_summary.md` §3.1 "How to address it" — 5-step length-confound remediation checklist (length-only baseline, stratified reporting, rebalancing, permanent baseline reporting, adversarial training as last resort). Built a companion `docs/phase1_eda_summary.html` — designed, self-contained HTML rendering of the same report (embeds the 2 key figures as base64, light/dark theme tokens) — and published it as a Claude Artifact for shareable viewing: https://claude.ai/code/artifact/fc67191a-7257-46a5-ad8a-6becf785f137 |
| 2026-07-09 | plan | — | Human requested dataset combinations (D1=D-SCRIPT PPI, D2=AVIDa no-COVID, D3=D1∪D2, D4=D3 as train pool + MLAEP as a held-out eval partition), pulling forward the item previously deferred to Phase 2+. Clarified two ambiguities via AskUserQuestion before building: "held out" = MLAEP as held-out eval (not merged), and MLAEP bridged into the pair schema via (RBD_mutant, human_ACE2, ace2_bind). Recorded as `spec/spec.md` "Revision 2026-07-09". |
| 2026-07-09 | step | — | Built D1-D4 via `src/spikes/build_combined_datasets.py`: D1=716,517 rows, D2=579,471, D3=1,295,988, D4-heldout=19,132. Fetched human ACE2 (UniProt Q9BYF1) live for the held-out pairing. All row counts verified against source-file sums. |
| 2026-07-09 | qa | — | Ran EDA (`src/spikes/eda_combined.py` → `docs/eda-combined.md`). **Key check: zero sequence overlap between D4's held-out set and the D3 training pool, both directions — held-out eval is genuinely clean.** Other findings: 1 shared sequence between D1/D2 (human TNF-alpha, not a leakage concern); D4 held-out has zero length variance (RBD mutants constant 201aa, ACE2 constant 805aa — 5aa past D1's 800aa training cap); `seq_a`/`seq_b` column semantics differ by `pair_type` in D3 (symmetric in PPI rows, antibody/antigen-asymmetric in AVIDa rows); D3 duplicate rows (3,886) inherited from D1's known ecoli duplicates, not new. See `dax-state/runs/combined-datasets-2026-07-09.md`. |
| 2026-07-09 | note | — | Human asked to try sourcing real sequences for MLAEP's 8-antibody escape panel (COV2-2050/2096/2094/2677/2479/2165/2499/2832). Original paper (Zost et al. 2020) gates them behind "available upon reasonable request"; Vanderbilt/Crowe patents only cover the 2 clinical-candidate clones (not ours); PDB search hit only false positives. **Found all 8 in CoV-AbDab** (Oxford OPIG's curated coronavirus antibody DB), correctly cited to Zost et al. 2020, with populated VH/VL columns. |
| 2026-07-09 | decision | — | Human confirmed: flip MLAEP's native escape-column polarity (1=fails to bind) to `1=binds`, for label consistency with D1/D2/D4-ACE2 (all use 1=positive interaction). Applied as `label = 1 - COV2-XXXX_400` in the build script. |
| 2026-07-09 | step | — | Built `d4_heldout_mlaep_antibodies.csv` (153,056 rows = 8 antibodies × 19,132 RBD mutants). `seq_b` = VH + "/" + VL per antibody (explicit chain-boundary separator). Verified zero sequence overlap with D3 training pool (both antibody and RBD-mutant sides); all VH/VL fully standard-alphabet; flipped binds-fractions (82-96% per clone) cross-checked as exactly 1-escape_fraction against original MLAEP numbers — exact match for all 8. Updated `docs/eda-combined.md` + `rawdata/combined/SOURCE.md`. See `dax-state/runs/combined-datasets-2026-07-09.md`. |
| 2026-07-09 | note | — | Built `docs/eda-combined.html` — designed companion HTML report for the combined-dataset EDA (same design system as `phase1_eda_summary.html`): stat tiles, dataset-definition cards, held-out cleanliness callouts for both axes, antibody-panel table/figure. Published as a Claude Artifact: https://claude.ai/code/artifact/4bea2a52-c761-4187-8d1b-4c98f5ee1dde |
| 2026-07-09 | step | — | Built + ran `src/spikes/length_baseline.py`: a GradientBoostingClassifier using ONLY length-derived features (len_a, len_b, sum, diff, product) — no sequence content — evaluated on D1's real human_train/human_test split and a stratified 80/20 split for D2 (no official split exists). |
| 2026-07-09 | qa | — | **Length-only baseline results: length carries real, exploitable signal in all 3 cases, not just PPI.** D1 (PPI): AUROC 0.652, AUPRC 0.183 vs 0.091 floor (2x). D2-hIL6: AUROC **0.803**, AUPRC 0.144 vs 0.037 floor (3.9x) — stronger than PPI. D2-hTNFa: AUROC 0.762, AUPRC 0.399 vs 0.122 floor (3.3x, small n=4,464/1,116, higher variance). Checked whether hIL6's result is antigen-identity-via-length: per-antigen mean VHH length only spans 150.8-151.2aa (r=-0.39 to positive rate) — not sufficient to explain AUROC 0.80 alone; more likely explanation is CDR3-length-correlated binding promiscuity at the individual-VHH level (a known antibody-literature phenomenon), not purely an assay artifact. See `docs/length_baseline_results.md`. |
