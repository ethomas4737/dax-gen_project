# Phase 2 plan — PLM baseline modeling (3 models × 3 datasets, D4 held-out)

## Current state
**Last updated:** 2026-07-10
**Load-bearing as of this date:** Real scope is 9 training runs (M1/M2/M3 × D1/D2/D3) + a D4 held-out eval pass on all 9 — see `spec/spec.md` Revision 2026-07-10b. **D4 (RBD-ACE2 + antibody escape) is the headline/primary success metric** (2026-07-10 decision) — D1/D2/D3 training and the cross-species eval exist in service of that goal, not as ends in themselves. Steps 1 and 6 are **reopened** (2026-07-10) to add a dedicated validation split and real per-epoch monitoring — see step 1c and the updated step 6 row.
**What's new since last update:** Human confirmed cross-species PPI (step 1b) stays a held-out internal test set, never trained on. Decided per-epoch validation monitoring during training must come from a fresh split carved out of `human_train` (not `human_test`, which stays untouched until final reporting) — new step **1c**. Clarified the real downstream objective is RBD-PPI prediction, making D4 the primary metric. See `dax-state/decisions.md` 2026-07-10 entry for full rationale. Execution order narrowing from earlier 2026-07-10 (D1-only near-term focus, D2/D3 deferred) still stands.

Status defaults to `not started`; update on step start/complete per the operational protocol in `CLAUDE.md`.

| Step ID | Status | Description | Inputs | Outputs | Role | Deps | Notes (plan-time, ≤200 chars) | Outcome (≤80 chars) |
|---|---|---|---|---|---|---|---|---|
| 0 | done | Build M1 pipeline scaffold (data prep, model, train, eval) + CPU smoke test, on D1's human-species subset only | `rawdata/ppi/pairs/human_*.tsv`, `seqs/human.fasta` | `src/spikes/phase2/{data_prep,model,train_m1,evaluate}.py` | executor | — | Scope now known to be too narrow for "D1" per spec — see step 1. | done — see runs/phase2-m1-d1-pipeline.md (superseded by step 1 for real D1 scope) |
| 1 | done | Define D1 train/test. **Corrected 2026-07-10:** only `human` has a real train/test split — mouse/fly/yeast/worm/ecoli ship one file each, used in D-SCRIPT's own design purely as zero-shot cross-species-transfer eval, not additional training data (matches Phase 1's established finding). So D1-train = `human_train`, D1-test = `human_test` only — **already curated in step 0.** No new data-prep code needed for D1 itself. | `rawdata/ppi/pairs/human_{train,test}.tsv` + `seqs/human.fasta` | `data/curated/d1_ppi/{train,test}.csv` (419,916 / 52,424 rows, already built) | executor | 0 | Supersedes the earlier "concatenate per-species splits" framing, which wrongly assumed every species has a train file. | done — see runs/phase2-m1-d1-pipeline.md |
| 1-qa | not started | QA: independent re-verification of step 0's dedup/bad-residue counts (still outstanding regardless of the species-scope correction) | step 1 outputs | qa note | qa-executor | 1 | | not started |
| 1b | not started | Curate mouse/fly/yeast/worm/ecoli as a standalone cross-species-transfer **internal test set** (confirmed 2026-07-10: eval-only, never trained on), analogous to D4 | `rawdata/ppi/pairs/{mouse,fly,yeast,worm,ecoli}_test.tsv` + `seqs/*.fasta` | `data/curated/d1_crossspecies_eval/{species}.csv` or similar | executor | 0 | Not part of D1's core train/test. Secondary to D4 (the headline metric) but still a real internal eval axis. Doesn't block the D1-only 3 runs (8a/9a). | not started |
| 1c | not started | Carve a dedicated validation split out of `human_train` (proposed default: 85/15 stratified by label, fixed seed) for per-epoch monitoring; `human_test` stays untouched until final reporting | `data/curated/d1_ppi/train.csv` | `data/curated/d1_ppi/{train,val}.csv` (train re-split) | executor | 1 | **New 2026-07-10** per decisions.md. Split ratio/stratification is a proposed default, not yet confirmed by human. | not started |
| 2 | deferred | Build D2 curated train/test split (AVIDa hIL6+hTNFa, no official split exists); **decided:** fresh stratified 80/20 by label, saved as a reusable artifact | `rawdata/combined/d2_avida.csv` | `data/curated/d2_avida/{train,test}.csv` | executor | — | **Deferred 2026-07-10** — human is weighing alternatives to D2, doesn't want this started yet. Split scheme decision stands whenever it resumes. | deferred — see plan front-matter |
| 3 | deferred | Build D3 curated train/test split (D1 ∪ D2); **decided:** concatenation of D1's + D2's respective splits | step 1 + step 2 outputs | `data/curated/d3_combined/{train,test}.csv` | executor | 1, 2 | **Deferred 2026-07-10** (depends on step 2, itself deferred). | deferred — see plan front-matter |
| 4 | done | Implement M2: attention-pooling variant (single learned Linear(d_model,1) score per residue, masked softmax) | `model.py` | working `AttnPooling` class | executor | — | Verified: correct output shape, gradients flow to hidden states + pooling params, masked positions provably contribute zero (perturbation test). Flagged: M1's embed-once-cache trick does not carry over to M2 (pooling is now trainable) — needs addressing in step 6. | done — standalone unit-tested, not yet wired into a training script (step 6) |
| 5 | done | Implement M3: LoRA-wrapped trainable backbone via HuggingFace `peft`, r=8 (within the approved 8-16 range), targeting attention projections | `model.py` | `apply_lora()` + `LORA_TARGET_MODULES` | executor | — | `peft` compatibility check passed: ESM-C uses a **fused** QKV Linear (`attn.layernorm_qkv.1`, not separate q/k/v), + `attn.out_proj` — targeting those two suffixes gives exactly the 60 attention Linears (120 LoRA tensors, 1.38M trainable / 334M total params). End-to-end verified (`PairClassifier(backbone_trainable=True)` + LoRA backbone + MeanPooling + head): 120/120 LoRA tensors get gradients, 0 base backbone tensors do, head trains. `pip install peft` (0.19.1) into the `esmc` env (own env, not a shared tenant env). M1/M2's embed-once-cache trick still does NOT apply to M3 (trainable backbone) — needs its own training loop in step 6, and its own GPU resource estimate before step 8a. | done — verified standalone, r=8 default (adjustable) |
| 6 | in progress | Build `train_lora.py` (M3) separate from a generalized `train_frozen.py` (M1/M2, `--pooling mean\|attn`); **decided:** keep the two training loops separate, not one unified script | steps 0, 4, 5 | `train_frozen.py`, `train_lora.py`, shared `evaluate.py` | architect, executor | 0, 4, 5, 1c | **Assumption flagged, not explicitly specified by human:** M3 defaults to mean-pooling (`--pooling mean`) to isolate exactly one change from M1. **Reopened 2026-07-10:** both scripts need a real per-epoch validation loop against step 1c's val split (previously absent by design — no monitoring/early-stopping existed). | partial — smoke-tested without validation monitoring; reopened, see decisions.md |
| 7 | not started | Build a reusable D4 held-out eval harness (both axes: ACE2 + 8-antibody panel) that scores any of the 9 trained models | `rawdata/combined/d4_heldout_*.csv`, any trained checkpoint | `docs/d4_heldout_eval.py` or similar + per-model results | executor | 6 | Reuses Phase 1's D4 build logic; new part is scoring an arbitrary checkpoint against it. | not started |
| 8a | not started | Run 3 training jobs on GPU (`singhlab-gpu`): M1/M2/M3 × **D1 only** | step 1, 6 outputs | 3 trained checkpoints (D1) | executor | 1-qa, 6 | **Requires human approval before each GPU allocation request.** M3's run likely needs a separate, larger resource ask than M1/M2's. Near-term focus per 2026-07-10 narrowing. | not started |
| 8b | deferred | Run remaining 6 training jobs: M1/M2/M3 × D2, and × D3 | steps 2, 3, 6 outputs | 6 trained checkpoints (D2, D3) | executor | 1-qa, 2, 3, 6 | **Deferred with steps 2/3** — resumes once the D2 dataset question settles. | deferred — see plan front-matter |
| 9a | not started | Run D4 held-out eval (both axes) against the 3 D1-trained checkpoints | step 7, 8a outputs | per-model D4 results (D1-trained models) | executor | 7, 8a | | not started |
| 9b | deferred | Run D4 held-out eval against the 6 D2/D3-trained checkpoints | step 7, 8b outputs | per-model D4 results (D2/D3-trained models) | executor | 7, 8b | Deferred with step 8b. | deferred — see plan front-matter |
| 10 | not started | Evaluation report — initially covering the 3 D1 runs + their D4 eval; extend to cover D2/D3 once steps 2-3/8b/9b resume | steps 8a, 9a (later: 8b, 9b) | `docs/phase2_eval_report.md` | executor, reviewer | 9a | Per `phase1_eda_summary.md` §3.1 stratified-reporting requirement. Can ship a D1-only version first rather than waiting for all 9. | not started |

## Decisions (resolved 2026-07-10)

1. **Step 1:** concatenate each species' existing D-SCRIPT train/test split into one D1 train/test (not a fresh combined split).
2. **Step 2:** fresh stratified 80/20-by-label split for D2, saved as a reusable artifact under `data/curated/d2_avida/`.
3. **Step 3:** D3 = concatenation of D1's + D2's splits (not a fresh joint split).
4. **Step 5:** M3 uses HuggingFace `peft`, low rank (r=8-16), targeting attention q/k/v/o projections — pending a quick check that `peft` cleanly targets ESM-C's module names; custom LoRA is the fallback only if it doesn't.
5. **Step 6:** separate `train_frozen.py` (M1/M2) and `train_lora.py` (M3) rather than one unified script.
6. **Step 1b/1c/objective (2026-07-10):** cross-species PPI stays internal-eval-only, never trained on; per-epoch validation monitoring comes from a fresh split carved out of `human_train` (proposed 85/15 stratified, not yet confirmed), not `human_test`; D4 is the headline metric for Phase 2's eval report, not a secondary check. See `dax-state/decisions.md`.

After all step-QA passes: promote spike scripts from `src/spikes/phase2/` to `src/` per `../dax/agent-configs/promotion-rules.md`, append journal rows, update `session-handoff.md`, commit per `[<step-id>] <short action>`.
